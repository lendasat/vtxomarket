{
  "name": "interim-asset-indexer",
  "version": "1.0.0",
  "description": "Temporary asset VTXO indexer for vtxo.market — bridges the gap until arkd ships native asset filtering",
  "module": "src/index.ts",
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --target=bun --outfile=dist/indexer.js"
  },
  "dependencies": {
    "@noble/curves": "^2.0.1",
    "@noble/hashes": "^2.0.1",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
